fit:
  beans:
    packages:
    - 'modelengine.fel.tool.mcp.server'

mcp:
  server:
    request:
      timeout-seconds: 60
    ping:
      interval-seconds: 30
    streamable:
      disallow-delete: false